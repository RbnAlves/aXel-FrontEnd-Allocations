# Environment Configuration
# Copy this file to .env.local for local development overrides

# Optional: Override automatic environment detection
# Values: development | uat | production
# If not set, environment is auto-detected from hostname
# VITE_APP_ENV=uat

# Note: In Azure Web App, you can set VITE_APP_ENV as an Application Setting
# before the build runs to lock the environment at build time
